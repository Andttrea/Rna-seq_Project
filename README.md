## Introduction

This project analyzes RNA-seq data from study `SRP127181` to understand how activating mutations in the estrogen receptor alpha (ESR1/ERa) affect the transcriptional response to estrogen in T47D cells (ER+ breast cancer). Specifically, it compares D538G and Y537S mutations against Wild Type at two time points (4 and 24 hours).


## Project Structure

The project follows an organized structure to facilitate reproducibility:

```
Rna-seq_Project/
├── code/                   # R scripts for data download and analysis
│   ├── data_download.R     
│   ├── model.R            
│   └── DE_plots.R          
├── plots/                  # Plots generated by the analysis
├── processed-data/         # Intermediate data files
│   ├── rse_gene_SRP127181          # Original RSE object (unfiltered)
│   ├── rse_gene_SRP127181_filtered # RSE object filtered by expression
│   ├── model_matrix                # Model design matrix
│   └── dge                         # Normalized DGEList object
├── raw-data/               # Raw data (unprocessed)
│   └── raw_rse_gene_SRP127181      # RSE object downloaded from recount3
├── Results.md              # Detailed interpretation of results
└── README.md               
```

You can navigate directly to the folders here:
- [`code/`](code/)
- [`plots/`](plots/)
- [`processed-data/`](processed-data/)
- [`raw-data/`](raw-data/)

## Scripts (brief description)

- [`code/data_download.R`](code/data_download.R) - Data download using recount3 and saving to raw-data.
- [`code/model.R`](code/model.R) - Preparation of the expression object, filtering, normalization, and statistical modeling.
- [`code/DE_plots.R`](code/DE_plots.R) - Generation of figures and differential analysis plots.

Note: The project was based on the [LieberInstitute/template_project](https://github.com/LieberInstitute/template_project) template.

## Dependencies and Requirements

- **R** (version 4.2 or higher recommended).
- **Main packages**: recount3, edgeR, limma, ggplot2, pheatmap, variancePartition.

Example installation in R:
```
install.packages(c("ggplot2", "pheatmap"))
if (!requireNamespace("BiocManager", quietly = TRUE)) install.packages("BiocManager")
BiocManager::install(c("recount3", "edgeR", "limma", "variancePartition"))
```

## Analysis Methodology

### Data and Experimental Design
- **Project**: SRP127181
- **Cell Line**: T47D (ER+ breast cancer)
- **Experimental Conditions**:
  - Genotypes: Wild Type (WT), D538G mutant, Y537S mutant
  - Treatments: Ethanol (control) vs 10 nM Estrogen (E2)
  - Time points: 4 hours and 24 hours
- **Analyzed Genes**: 21,272 genes (after low expression filtering)
- **Original Genes**: 63,856 (33.31% retained after filtering)

### Analysis Pipeline
1. **Data Download**: Using the recount3 package.
2. **Filtering**: Removal of low-expression genes using filterByExpr (edgeR).
3. **Normalization**: TMM (Trimmed Mean of M-values) normalization factors.
4. **Transformation**: voom for variance stabilization.
5. **Statistical Model**: 
   ```
   ~ mutation_status + treatment_time + assigned_gene_prop
   ```
6. **Differential Analysis**: limma-eBayes with FDR < 0.05

---

The interpretation of the plots and findings can be found in [`Results.md`](Results.md)

